<% if book.favorited_by?(current_user) %>
  <td>
    <%= link_to book_favorites_path(book.id), method: :delete, remote: true do %>
      ♥<%= book.favorites.count %>いいね
    <% end %>
  </td>
<% else %>
  <td>
    <%= link_to book_favorites_path(book.id), method: :post, remote:  do %>
      ♡<%= book.favorites.count %>いいね
    <% end %>
  </td>
<% end %>